---
apiVersion: v1
data:
{{ (.Files.Glob "files/study-dashboard.json").AsConfig | indent 2 }}
kind: ConfigMap
metadata:
  name: study-mon-gf-dashboards-{{ .Release.Name }}
  namespace: "{{ .Values.global.services_namespace }}"
