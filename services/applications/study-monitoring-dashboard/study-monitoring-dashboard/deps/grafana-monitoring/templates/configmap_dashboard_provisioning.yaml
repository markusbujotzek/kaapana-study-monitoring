---
apiVersion: v1
data:
{{ (.Files.Glob "files/study.yaml").AsConfig | indent 2 }}
kind: ConfigMap
metadata:
  name: study-monitoring-gf-provisioning-dashboards-{{ .Release.Name }}
  namespace: "{{ .Values.global.services_namespace }}"

